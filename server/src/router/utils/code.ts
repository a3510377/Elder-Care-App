export enum ResponseStatus {
  SUCCESS,
  MISSING_ARGUMENT,
  NOT_FOUND,
  INVALID_USER_AVATAR_FILE,
}

export enum HttpStatus {
  OK = 200,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
}
